using UnityEngine;

public class Crosshair : MonoBehaviour
{
    /*
    //inputs
    private PlayerInput playerInput;
    private Camera camera;

    //vectors
    private Vector2 mousePosition;
    private Vector2 aim;

    private void Start()
    {
        camera = Camera.main;
        playerInput = InputManager.Instance.GetInputActions();
    }

    void Update()
    {
        CrossHair();
    }

    private void CrossHair()
    {
        mousePosition = playerInput.player.mousePosition.ReadValue<Vector2>();
        aim = camera.ScreenToWorldPoint(mousePosition);
        
        transform.position = aim;
    }*/
}